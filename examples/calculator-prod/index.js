#!/usr/bin/env node

/**
 * Calculator CLI Application Entry Point
 * 
 * This file serves as the main entry point for the calculator CLI application.
 * It initializes the CLI interface and handles the program execution.
 */

import { runCLI } from './src/cli.js';

// Run the CLI application
runCLI();